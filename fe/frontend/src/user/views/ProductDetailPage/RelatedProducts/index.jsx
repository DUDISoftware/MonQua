import React from 'react';
import RelatedProductsList from './RelatedProductsList.jsx';

const RelatedProducts = ({ products }) => {
    return <RelatedProductsList relatedProducts={products} />;
};

export default RelatedProducts;
